Place packaged `trajectory-client` sidecar binaries here for release builds.

During development the desktop controller also searches the repository
`target/release` and `target/debug` directories, or uses `TRAJECTORY_CLIENT_BIN`
when set.
